# Geoblinko 生产环境配置示例
# 复制此文件为 .env.prod 并修改为实际值

# ============================================
# 数据库配置（强烈建议修改）
# ============================================
POSTGRES_DB=postgres
POSTGRES_USER=postgres
# ⚠️ 必须修改：请使用强密码（至少 16 位，包含大小写字母、数字和特殊字符）
POSTGRES_PASSWORD=your_secure_password_here_change_me

# 数据库连接字符串（容器内使用）
DATABASE_URL=postgresql://postgres:your_secure_password_here_change_me@postgres:5432/postgres

# ============================================
# 认证配置（必须修改）
# ============================================
# ⚠️ 必须修改：NextAuth 密钥（用于签名和加密）
# 生成方法：openssl rand -base64 32
NEXTAUTH_SECRET=your_random_nextauth_secret_here_change_me

# 应用访问 URL（根据实际情况修改）
NEXTAUTH_URL=http://localhost:2222
NEXT_PUBLIC_BASE_URL=http://localhost:2222

# ============================================
# 地图服务配置（支持国内外）
# ============================================
# 地图服务提供商
# auto: 自动选择（国内用高德，国外用Google）
# amap: 强制使用高德地图（仅中国地区）
# google: 强制使用 Google Maps（国际地区）
# osm: 使用 OpenStreetMap（免费，无需API Key）
MAP_PROVIDER=auto

# 高德地图 API Key（中国地区）
# 获取方式：访问 https://console.amap.com/ 创建应用并获取 Key
VITE_AMAP_WEB_API_KEY=your_amap_api_key_here
NEXT_PUBLIC_AMAP_WEB_API_KEY=your_amap_api_key_here
AMAP_WEB_API_KEY=your_amap_api_key_here

# Google Maps API Key（国际地区）
# 获取方式：访问 https://console.cloud.google.com/ 创建凭据并获取 Key
# 需要启用：Maps JavaScript API, Places API, Geocoding API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ============================================
# 性能优化参数（可选）
# ============================================
# 根据服务器内存调整（默认: 256MB）
# 建议: 4GB 内存 = 1GB, 8GB 内存 = 2GB
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_WORK_MEM=8MB
POSTGRES_MAX_CONNECTIONS=100
